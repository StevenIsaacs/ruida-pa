bokeh
pyserial
textual>=0.32  # TUI framework for interactive script execution

rpyc  # RPyC remote procedure call for TUI RPC

# Optional: for PNG export via Bokeh's export_png (requires headless Chrome/Firefox)
# selenium
